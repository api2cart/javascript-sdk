/**
 * API2Cart OpenAPI
 * API2Cart
 *
 * The version of the OpenAPI document: 1.1
 * Contact: contact@api2cart.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The CustomerConsent model module.
 * @module model/CustomerConsent
 * @version 1.1
 */
class CustomerConsent {
    /**
     * Constructs a new <code>CustomerConsent</code>.
     * @alias module:model/CustomerConsent
     */
    constructor() { 
        
        CustomerConsent.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>CustomerConsent</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CustomerConsent} obj Optional instance to populate.
     * @return {module:model/CustomerConsent} The populated <code>CustomerConsent</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CustomerConsent();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
            if (data.hasOwnProperty('source')) {
                obj['source'] = ApiClient.convertToType(data['source'], 'String');
            }
            if (data.hasOwnProperty('opt_in_level')) {
                obj['opt_in_level'] = ApiClient.convertToType(data['opt_in_level'], 'String');
            }
            if (data.hasOwnProperty('modified_time')) {
                obj['modified_time'] = ApiClient.convertToType(data['modified_time'], 'String');
            }
            if (data.hasOwnProperty('additional_fields')) {
                obj['additional_fields'] = ApiClient.convertToType(data['additional_fields'], Object);
            }
            if (data.hasOwnProperty('custom_fields')) {
                obj['custom_fields'] = ApiClient.convertToType(data['custom_fields'], Object);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CustomerConsent</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CustomerConsent</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }
        // ensure the json data is a string
        if (data['source'] && !(typeof data['source'] === 'string' || data['source'] instanceof String)) {
            throw new Error("Expected the field `source` to be a primitive type in the JSON string but got " + data['source']);
        }
        // ensure the json data is a string
        if (data['opt_in_level'] && !(typeof data['opt_in_level'] === 'string' || data['opt_in_level'] instanceof String)) {
            throw new Error("Expected the field `opt_in_level` to be a primitive type in the JSON string but got " + data['opt_in_level']);
        }
        // ensure the json data is a string
        if (data['modified_time'] && !(typeof data['modified_time'] === 'string' || data['modified_time'] instanceof String)) {
            throw new Error("Expected the field `modified_time` to be a primitive type in the JSON string but got " + data['modified_time']);
        }

        return true;
    }


}



/**
 * @member {String} id
 */
CustomerConsent.prototype['id'] = undefined;

/**
 * @member {String} type
 */
CustomerConsent.prototype['type'] = undefined;

/**
 * @member {String} status
 */
CustomerConsent.prototype['status'] = undefined;

/**
 * @member {String} source
 */
CustomerConsent.prototype['source'] = undefined;

/**
 * @member {String} opt_in_level
 */
CustomerConsent.prototype['opt_in_level'] = undefined;

/**
 * @member {String} modified_time
 */
CustomerConsent.prototype['modified_time'] = undefined;

/**
 * @member {Object} additional_fields
 */
CustomerConsent.prototype['additional_fields'] = undefined;

/**
 * @member {Object} custom_fields
 */
CustomerConsent.prototype['custom_fields'] = undefined;






export default CustomerConsent;

