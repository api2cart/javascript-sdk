/**
 * API2Cart OpenAPI
 * API2Cart
 *
 * The version of the OpenAPI document: 1.1
 * Contact: contact@api2cart.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ProductVariantAddBatchPayloadInner from './ProductVariantAddBatchPayloadInner';

/**
 * The ProductVariantAddBatch model module.
 * @module model/ProductVariantAddBatch
 * @version 1.1
 */
class ProductVariantAddBatch {
    /**
     * Constructs a new <code>ProductVariantAddBatch</code>.
     * @alias module:model/ProductVariantAddBatch
     * @param payload {Array.<module:model/ProductVariantAddBatchPayloadInner>} Contains an array of product variants objects. The list of properties may vary depending on the specific platform.
     */
    constructor(payload) { 
        
        ProductVariantAddBatch.initialize(this, payload);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, payload) { 
        obj['clear_cache'] = false;
        obj['reindex'] = false;
        obj['payload'] = payload;
    }

    /**
     * Constructs a <code>ProductVariantAddBatch</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ProductVariantAddBatch} obj Optional instance to populate.
     * @return {module:model/ProductVariantAddBatch} The populated <code>ProductVariantAddBatch</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ProductVariantAddBatch();

            if (data.hasOwnProperty('clear_cache')) {
                obj['clear_cache'] = ApiClient.convertToType(data['clear_cache'], 'Boolean');
            }
            if (data.hasOwnProperty('reindex')) {
                obj['reindex'] = ApiClient.convertToType(data['reindex'], 'Boolean');
            }
            if (data.hasOwnProperty('payload')) {
                obj['payload'] = ApiClient.convertToType(data['payload'], [ProductVariantAddBatchPayloadInner]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ProductVariantAddBatch</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ProductVariantAddBatch</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ProductVariantAddBatch.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['payload']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['payload'])) {
                throw new Error("Expected the field `payload` to be an array in the JSON data but got " + data['payload']);
            }
            // validate the optional field `payload` (array)
            for (const item of data['payload']) {
                ProductVariantAddBatchPayloadInner.validateJSON(item);
            };
        }

        return true;
    }


}

ProductVariantAddBatch.RequiredProperties = ["payload"];

/**
 * @member {Boolean} clear_cache
 * @default false
 */
ProductVariantAddBatch.prototype['clear_cache'] = false;

/**
 * @member {Boolean} reindex
 * @default false
 */
ProductVariantAddBatch.prototype['reindex'] = false;

/**
 * Contains an array of product variants objects. The list of properties may vary depending on the specific platform.
 * @member {Array.<module:model/ProductVariantAddBatchPayloadInner>} payload
 */
ProductVariantAddBatch.prototype['payload'] = undefined;






export default ProductVariantAddBatch;

